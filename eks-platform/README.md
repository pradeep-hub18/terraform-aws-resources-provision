# EKS Platform

Live Terraform configuration for EKS platform controllers:

- AWS Load Balancer Controller
- EBS CSI and encrypted gp3 StorageClass
- Istio
- Argo CD
- External Secrets Operator
- Metrics Server

Apply this stack after `networking`, `eks`, and `edge-routing`.
